
version: 2

models:
  - name: stg_listings
    description:

    columns:
      - name: listing_id
        description:
        tests:
          - not_null
          - unique

models:
  - name: stg_reviews
    description:

    columns:
      - name: review_id
        description:
        tests:
          - not_null
          - unique
